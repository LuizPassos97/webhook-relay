## Summary

<!-- What does this change and why? Link related issues. -->

## Verification

<!-- How did you verify it? Mention new or changed tests. -->

- [ ] `npm run lint`, `npm run typecheck` and `npm run build` pass
- [ ] `npm run test:coverage` passes, with a test that fails without this change
- [ ] Documentation and `docs/openapi.json` are updated if behavior or routes changed
- [ ] No secrets, API keys or payloads in code, logs or test fixtures
